apiVersion: v1
kind: Service
metadata:
  name: app-a
  namespace: default
  labels:
    app: app-a
spec:
  type: ClusterIP
  selector:
    app: app-a
  ports:
  - name: https
    port: 8443
    targetPort: 8443
    protocol: TCP
